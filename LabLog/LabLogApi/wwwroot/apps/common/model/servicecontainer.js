define(['ko', 'cookies', 'utils'],
    function (ko, Cookies, utils) {
        var singleton = function serviceContainer() {
            var self = this;
        };
        return new singleton();
    }
);